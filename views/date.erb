<!DOCTYPE html>

<html>
  <% day = Time.now.strftime("%A")
     month = Time.now.strftime("%B")
     date = Time.now.strftime("%d")
     year = Time.now.strftime("%G") %>
  <p><%= "The date is #{day}, #{month} #{date}, #{year}" %></p>
</html>